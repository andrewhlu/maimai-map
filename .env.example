# Copy this file to `.env` and fill in your key.
# `.env` is gitignored so your key won't be committed.

# Google Maps Platform API key for the Places API (New) — Text Search.
# See README "Geocode (Google Places fallback)" for setup instructions.
GOOGLE_MAPS_API_KEY=
